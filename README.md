# OpenTSDB linked to HBase

Start an OpenTSDB instance attached to an existing HBase instance

Requires a link to "hbase" service

Will wait for HBase to be ready, create the OpenTSDB tables, and run OpenTSDB